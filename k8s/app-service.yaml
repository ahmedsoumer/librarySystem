apiVersion: v1
kind: Service
metadata:
  name: library-app-service
  namespace: library-system
  labels:
    app: library-app
spec:
  selector:
    app: library-app
  ports:
  - port: 8081
    targetPort: 8081
    protocol: TCP
    name: http
  type: ClusterIP
